<?php

use Illuminate\Database\Seeder;

class UserMarketRequestTradeablesSeeder extends Seeder
{
    /**
     * Run the database seeds.
     *
     * @return void
     */
    public function run()
    {
        // $userMarketRequests = App\Models\MarketRequest\UserMarketRequest::all();
        // $markets = App\Models\StructureItems\Market::where('parent_id',null)->get();
        // foreach($markets as $market) {
        //     foreach($userMarketRequests as $marketRequest) {
        //         factory(App\Models\MarketRequest\UserMarketRequestTradable::class)->create([
        //             "user_market_request_id"    => $marketRequest->id,
        //             "market_id"                 => $market->id
        //         ]);
        //     }
        // }

    }
}
