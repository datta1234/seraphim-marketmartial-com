import { mount } from '@vue/test-utils';
import FilterMarketsMenu from '../../../resources/assets/js/components/ActionBar/Components/FilterMarketsMenuComponent.vue';

describe('FilterMarketsMenu.vue', () => {

	describe('Viewing Filter Markets', () => {

		it('Add a filter market to display', () => {
			chai.assert(false, "TODO");
		});

		it('Remove a filter market to display', () => {
			chai.assert(false, "TODO");
		});

		it('Creates a boolean list of available markets', () => {
			chai.assert(false, "TODO");
		});

		it('Filter Markets Menu emits close when dismissing a popover', () => {
			chai.assert(false, "TODO");
		});

		it('Compiles a list of Important Markets from the passed market prop', () => {
			chai.assert(false, "TODO");
		});
	});
});